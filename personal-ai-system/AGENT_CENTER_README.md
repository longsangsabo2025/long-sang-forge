# 🤖 AI Agent Center

**Trung Tâm AI Agent Toàn Diện với LangGraph, CrewAI & Multi-Framework Integration**

---

## 🌟 Tổng Quan

AI Agent Center là một hệ thống orchestration toàn diện cho phép bạn:

- 🔄 **Orchestrate workflows** phức tạp với LangGraph
- 🤝 **Phối hợp nhiều agents** cùng làm việc với CrewAI
- 🛠️ **Sử dụng 100+ tools** được tích hợp sẵn
- 📊 **Giám sát & phân tích** hiệu suất real-time
- 🚀 **Mở rộng dễ dàng** với custom agents và tools
- 🌐 **API-first design** để tích hợp mọi nơi

---

## 📦 Features

### Core Capabilities

✅ **Multi-Framework Support**
- LangGraph: Stateful workflow orchestration
- CrewAI: Role-based multi-agent collaboration  
- LangChain: 100+ pre-built tools
- AutoGen: Conversational agents
- Semantic Kernel: Microsoft ecosystem

✅ **Workflow Patterns**
- Sequential: A → B → C
- Parallel: A, B, C simultaneously
- Conditional: Branching logic
- Pipeline: Data transformation
- Map-Reduce: Distribute & aggregate

✅ **Agent Management**
- Dynamic agent registration
- Agent lifecycle management
- Capability-based routing
- Performance monitoring

✅ **Tool Ecosystem**
- 100+ integrated tools
- Custom tool registration
- Category-based organization
- Usage tracking & analytics

✅ **Production Ready**
- RESTful API
- Health monitoring
- Error handling
- Cost tracking
- LangSmith integration

---

## 🚀 Quick Start

### 1. Setup (5 phút)

```bash
cd personal-ai-system

# Run automated setup
python setup_agent_center.py

# Configure API keys
cp .env.example .env
# Edit .env and add your keys
```

### 2. Run Your First Workflow (2 phút)

```bash
# Sequential workflow example
python examples/sequential_workflow_example.py

# Output:
# ✅ Workflow completed!
# Status: completed
# Results: {...}
```

### 3. Try CrewAI Multi-Agent (3 phút)

```bash
# Content creation with 3 agents
python examples/crewai_example.py full

# Output:
# ✅ Content created successfully!
# Generated by: Researcher → Writer → Editor
```

### 4. Start API Server

```bash
# Start server
python -m uvicorn api.main:app --reload

# Access docs
open http://localhost:8000/docs
```

**Total time: 10 phút ⚡**

---

## 💡 Use Cases

### 1. Content Marketing Automation
```python
# Research → Outline → Write → Edit → SEO → Publish
builder.sequential(
    name="content_pipeline",
    steps=[
        ("research", "research_agent"),
        ("outline", "work_agent"),
        ("write", "work_agent"),
        ("edit", "work_agent"),
        ("seo", "work_agent")
    ]
)
```

### 2. Multi-Channel Campaign
```python
# Create content for all channels at once
builder.parallel(
    name="campaign",
    parallel_steps=[
        ("blog", "work_agent"),
        ("social", "work_agent"),
        ("email", "work_agent"),
        ("newsletter", "work_agent")
    ],
    aggregator=("report", "research_agent")
)
```

### 3. Adaptive Content
```python
# Branch based on complexity
builder.conditional(
    name="adaptive",
    initial_step=("analyze", "research_agent"),
    condition=lambda state: state.context["complexity"],
    branches={
        "simple": [("quick_write", "work_agent")],
        "detailed": [("research", "research_agent"), 
                    ("write", "work_agent")]
    }
)
```

### 4. Data Analysis Pipeline
```python
# Collect → Clean → Analyze → Visualize → Report
WorkflowTemplates.data_analysis_workflow(orchestrator)
```

---

## 📚 Documentation

### Tài Liệu Chính
- **[AI_AGENT_CENTER_PLAN.md](../AI_AGENT_CENTER_PLAN.md)** - Kế hoạch chi tiết, kiến trúc, roadmap
- **[AI_AGENT_CENTER_QUICKSTART.md](../AI_AGENT_CENTER_QUICKSTART.md)** - Hướng dẫn nhanh với examples
- **[AI_AGENT_CENTER_IMPLEMENTATION_SUMMARY.md](../AI_AGENT_CENTER_IMPLEMENTATION_SUMMARY.md)** - Tóm tắt triển khai

### Code Examples
- `examples/sequential_workflow_example.py` - Sequential workflow
- `examples/crewai_example.py` - Multi-agent collaboration

### API Documentation
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

---

## 🏗️ Architecture

```
┌──────────────────────────────────────────┐
│         FastAPI REST API                 │
│    /v1/agent-center/* endpoints          │
└──────────────┬───────────────────────────┘
               │
     ┌─────────┴─────────┐
     │                   │
┌────▼──────┐    ┌───────▼────────┐
│ LangGraph │    │  Agent Manager │
│Orchestrator│    │   & Registry   │
└────┬──────┘    └───────┬────────┘
     │                   │
     │    ┌──────────────┴───────┐
     │    │                      │
┌────▼────▼───┐          ┌───────▼─────┐
│   Agents    │          │    Tools    │
│ Work/Life/  │          │  Registry   │
│  Research   │          │  100+ tools │
└────┬────────┘          └───────┬─────┘
     │                           │
     └───────────┬───────────────┘
                 │
        ┌────────▼─────────┐
        │  CrewAI Crews    │
        │ Multi-Agent      │
        │ Collaboration    │
        └──────────────────┘
```

---

## 🔧 Core Components

### 1. LangGraph Orchestrator
**File**: `core/orchestrator/langgraph_orchestrator.py`

```python
orchestrator = LangGraphOrchestrator()
orchestrator.register_agent("my_agent", agent)
orchestrator.create_workflow("workflow_name")
# Add nodes, edges, compile
result = await orchestrator.execute(task)
```

### 2. Workflow Builder
**File**: `core/orchestrator/workflow_builder.py`

```python
builder = WorkflowBuilder(orchestrator)
builder.sequential(name, steps)
builder.parallel(name, steps, aggregator)
builder.conditional(name, initial, condition, branches)
```

### 3. CrewAI Integration
**File**: `agents/specialized/content_creator_crew.py`

```python
crew = ContentCreatorCrew()
result = await crew.create_content(topic, keywords, tone)
```

### 4. Tool Registry
**File**: `core/tools/enhanced_registry.py`

```python
registry = get_global_registry()
@registry.register_tool(category, description)
def my_tool(input): ...
```

### 5. Management API
**File**: `api/agent_center.py`

```bash
GET  /v1/agent-center/agents
POST /v1/agent-center/workflows/execute
GET  /v1/agent-center/tools
GET  /v1/agent-center/analytics/overview
```

---

## 🎯 API Endpoints

### Agents
```bash
GET    /v1/agent-center/agents              # List all
GET    /v1/agent-center/agents/{name}       # Get details
```

### Workflows
```bash
POST   /v1/agent-center/workflows/execute   # Execute
GET    /v1/agent-center/workflows/history   # History
GET    /v1/agent-center/workflows/execution/{id}
```

### CrewAI
```bash
POST   /v1/agent-center/crews/execute       # Execute crew
POST   /v1/agent-center/crews/content/research
```

### Tools
```bash
GET    /v1/agent-center/tools               # List all
GET    /v1/agent-center/tools/{name}        # Details
GET    /v1/agent-center/tools/search?query=...
GET    /v1/agent-center/tools/categories
```

### Analytics
```bash
GET    /v1/agent-center/analytics/overview
GET    /v1/agent-center/analytics/tools/usage
```

### Health
```bash
GET    /v1/agent-center/health              # Health check
GET    /v1/agent-center/status              # System status
```

---

## 🛠️ Customization

### Add Custom Agent

```python
from core.base_agent import BaseAgent

class MyAgent(BaseAgent):
    def __init__(self):
        super().__init__(name="my_agent", role="Custom")
    
    async def process(self, input_data):
        # Your logic
        return {"response": "..."}

# Register
orchestrator.register_agent("my_agent", MyAgent())
```

### Add Custom Tool

```python
from core.tools.enhanced_registry import get_global_registry

registry = get_global_registry()

@registry.register_tool(
    category=ToolCategory.UTILITY,
    description="My custom tool"
)
def my_tool(input: str) -> str:
    return f"Result: {input}"
```

### Create Custom Crew

```python
from crewai import Agent, Task, Crew

analyst = Agent(role="Analyst", goal="...", tools=[...])
reporter = Agent(role="Reporter", goal="...", tools=[...])

crew = Crew(
    agents=[analyst, reporter],
    tasks=[analysis_task, reporting_task]
)

result = crew.kickoff()
```

---

## 📊 Monitoring

### LangSmith Integration

```python
# Set in .env
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your_key
LANGCHAIN_PROJECT=your_project

# All executions auto-traced
# View at: https://smith.langchain.com/
```

### Analytics API

```bash
# System overview
curl http://localhost:8000/v1/agent-center/analytics/overview

# Tool usage
curl http://localhost:8000/v1/agent-center/analytics/tools/usage
```

---

## 🧪 Testing

### Run Examples

```bash
# All examples
python examples/sequential_workflow_example.py
python examples/crewai_example.py

# Specific CrewAI modes
python examples/crewai_example.py full
python examples/crewai_example.py research
python examples/crewai_example.py write
```

### API Testing

```bash
# Start server
python -m uvicorn api.main:app --reload

# Test health
curl http://localhost:8000/v1/agent-center/health

# List agents
curl http://localhost:8000/v1/agent-center/agents

# Execute workflow
curl -X POST http://localhost:8000/v1/agent-center/workflows/execute \
  -H "Content-Type: application/json" \
  -d '{"workflow_type": "sequential", "task": "Write about AI"}'
```

---

## 📦 Dependencies

### Core Frameworks
```
langgraph>=0.2.0
langchain>=0.3.0
crewai>=0.11.0
pyautogen>=0.2.0
semantic-kernel>=1.0.0
```

### Installation
```bash
pip install -r requirements-aiagent.txt
```

---

## 🐛 Troubleshooting

### Import Errors
```bash
pip install -r requirements-aiagent.txt --upgrade
```

### Missing API Keys
```bash
# Check .env
cat .env | grep API_KEY

# Set environment
export OPENAI_API_KEY=your_key
```

### Enable Debug Logging
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

---

## 🎓 Learning Path

### Beginner (Week 1)
1. ✅ Run setup script
2. ✅ Try sequential workflow example
3. ✅ Explore API documentation
4. ✅ Modify an example

### Intermediate (Week 2-3)
1. 🔨 Create custom agent
2. 🔨 Build custom workflow
3. 🔨 Add custom tools
4. 🔨 Integrate with existing code

### Advanced (Week 4+)
1. 🚀 Build custom CrewAI crew
2. 🚀 Implement complex workflows
3. 🚀 Add monitoring dashboard
4. 🚀 Production deployment

---

## 🤝 Contributing

### Add Agent
1. Extend `BaseAgent`
2. Implement `process()` method
3. Register with orchestrator
4. Add tests & documentation

### Add Tool
1. Use `@registry.register_tool()` decorator
2. Specify category & metadata
3. Add unit tests
4. Update documentation

### Add Workflow Template
1. Add to `WorkflowTemplates` class
2. Test thoroughly
3. Create example usage
4. Document

---

## 📈 Roadmap

### ✅ Phase 1: Foundation (Completed)
- LangGraph orchestration
- CrewAI integration
- Tool registry
- Management API
- Examples & docs

### 🔄 Phase 2: Enhancement (Current)
- [ ] Frontend dashboard
- [ ] More workflow templates
- [ ] Additional crews
- [ ] Advanced monitoring

### 🎯 Phase 3: Scale (Future)
- [ ] Multi-tenancy
- [ ] Plugin marketplace
- [ ] Advanced analytics
- [ ] Mobile app

---

## 💰 Cost Optimization

### Tips
1. **Use appropriate models**: GPT-4o for complex, GPT-3.5-turbo for simple
2. **Cache results**: Use Redis for frequently accessed data
3. **Batch operations**: Leverage parallel workflows
4. **Monitor usage**: Track with analytics API
5. **Set limits**: Configure rate limits and quotas

### Cost Tracking
```python
# Get cost estimation
registry = get_global_registry()
cost = registry.estimate_cost(["tool1", "tool2", "tool3"])
print(f"Estimated cost: ${cost:.4f}")
```

---

## 📞 Support & Resources

### Documentation
- 📖 [Comprehensive Plan](../AI_AGENT_CENTER_PLAN.md)
- ⚡ [Quick Start](../AI_AGENT_CENTER_QUICKSTART.md)
- 📝 [Implementation Summary](../AI_AGENT_CENTER_IMPLEMENTATION_SUMMARY.md)

### External Resources
- **LangGraph**: https://langchain-ai.github.io/langgraph/
- **CrewAI**: https://docs.crewai.com/
- **LangChain**: https://python.langchain.com/

### Community
- LangChain Discord
- CrewAI Discord
- GitHub Issues

---

## ⭐ Key Highlights

✨ **Production Ready**: Full API, monitoring, error handling  
🚀 **Multi-Framework**: LangGraph, CrewAI, AutoGen, Semantic Kernel  
🛠️ **100+ Tools**: Pre-integrated and ready to use  
📊 **Analytics**: Real-time monitoring and cost tracking  
🔧 **Extensible**: Easy to add agents, tools, and workflows  
📚 **Well Documented**: Comprehensive guides and examples  
⚡ **Fast Setup**: 10 minutes to first workflow  

---

## 🎉 Getting Started

```bash
# 1. Setup
cd personal-ai-system
python setup_agent_center.py

# 2. Try it
python examples/sequential_workflow_example.py

# 3. Start API
python -m uvicorn api.main:app --reload

# 4. Build something amazing! 🚀
```

---

**Built with ❤️ using LangGraph, CrewAI, and the power of AI agents**

Version: 1.0  
Status: ✅ Production Ready  
Last Updated: January 2025
